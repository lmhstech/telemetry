diff --git a/public/404.html b/public/404.html
index e129fc4..2133aec 100644
--- a/public/404.html
+++ b/public/404.html
@@ -5,6 +5,7 @@
 <meta name="viewport" content="width=device-width, initial-scale=1">
 <title>Not found · Personal Finance · Room 1-240</title>
 <link rel="stylesheet" href="/styles.css">
+  <script src="/telemetry.js"></script>
 <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' rx='22' fill='%231a7f5a'/%3E%3Ctext x='50' y='72' font-size='62' font-family='Georgia,serif' font-weight='700' text-anchor='middle' fill='%23fff'%3E%24%3C/text%3E%3C/svg%3E">
 <script>
 /* Runs before first paint. app.js is deferred, so leaving this to it meant a
diff --git a/public/index.html b/public/index.html
index df9e7bc..b17dcda 100644
--- a/public/index.html
+++ b/public/index.html
@@ -6,6 +6,7 @@
 <title>Personal Finance · Room 1-240 · LMHS</title>
 <meta name="description" content="The Personal Finance courses in Room 1-240 at LMHS — what the class covers, how money actually works, and the tools to try it yourself.">
 <link rel="stylesheet" href="/styles.css">
+  <script src="/telemetry.js"></script>
 <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' rx='22' fill='%231a7f5a'/%3E%3Ctext x='50' y='72' font-size='62' font-family='Georgia,serif' font-weight='700' text-anchor='middle' fill='%23fff'%3E%24%3C/text%3E%3C/svg%3E">
 <script>
 /* Runs before first paint. app.js is deferred, so leaving this to it meant a
diff --git a/public/resources.html b/public/resources.html
index 0f60ccf..e3db7ec 100644
--- a/public/resources.html
+++ b/public/resources.html
@@ -6,6 +6,7 @@
 <title>Tools &amp; glossary · Personal Finance · Room 1-240</title>
 <meta name="description" content="Plain-English definitions, rules of thumb, and trustworthy places to look things up — for the Personal Finance courses in Room 1-240 at LMHS.">
 <link rel="stylesheet" href="/styles.css">
+  <script src="/telemetry.js"></script>
 <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' rx='22' fill='%231a7f5a'/%3E%3Ctext x='50' y='72' font-size='62' font-family='Georgia,serif' font-weight='700' text-anchor='middle' fill='%23fff'%3E%24%3C/text%3E%3C/svg%3E">
 <script>
 /* Runs before first paint. app.js is deferred, so leaving this to it meant a
diff --git a/public/telemetry.js b/public/telemetry.js
new file mode 100644
index 0000000..f2b5ee3
--- /dev/null
+++ b/public/telemetry.js
@@ -0,0 +1,104 @@
+// Configuration. See main-site/js/telemetry.js for why a static site's ingest
+// key is public, and how it is revoked. Replace after registering
+// "finance-site" at telemetry.lmhstech.com/admin.
+window.LMHS_TELEMETRY = {
+  key: 'REPLACE_WITH_FINANCE_SITE_INGEST_KEY',
+  environment: 'production'
+};
+
+// Telemetry reporter for browser pages (main-site, finance-site, and the
+// dashboards' client-side code).
+//
+// Two things are different in a browser and both matter:
+//
+//   1. The ingest key ships to the client and is therefore public. That is
+//      accepted: the key authorises *writing a crash report*, nothing else,
+//      and it is revoked by rotating it in the admin page. Never give a
+//      browser the key of a server-side app.
+//   2. The page is full of things a student typed. So this sends the error
+//      and nothing else — no DOM, no form state, no localStorage.
+
+(function () {
+  'use strict';
+
+  var cfg = window.LMHS_TELEMETRY || {};
+  var KEY = cfg.key;
+  var ENDPOINT = cfg.endpoint || 'https://telemetry.lmhstech.com/api/ingest';
+  if (!KEY) return; // not configured on this page
+
+  function scrub(text) {
+    return String(text == null ? '' : text)
+      .replace(/[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}/g, '[email]')
+      .replace(/\beyJ[A-Za-z0-9_-]{4,}\.[A-Za-z0-9_-]{4,}\.[A-Za-z0-9_-]*/g, '[jwt]')
+      .replace(/\b\d{6,}\b/g, '[number]');
+  }
+
+  // Errors on a page arrive in bursts — one broken script can fire the same
+  // handler on every frame. Cap what any one page load will send.
+  var sent = 0;
+  var MAX_PER_PAGE = 5;
+  var seen = {};
+
+  function send(message, stack, extra) {
+    if (sent >= MAX_PER_PAGE) return;
+
+    // Collapse repeats within this page load before they leave the browser.
+    var dedupe = message + '|' + (extra && extra.line);
+    if (seen[dedupe]) return;
+    seen[dedupe] = true;
+    sent++;
+
+    var body = JSON.stringify({
+      message: scrub(message).slice(0, 1000),
+      stack: scrub(stack).slice(0, 4000) || undefined,
+      level: 'error',
+      environment: cfg.environment || 'production',
+      release: cfg.release,
+      context: {
+        // Path only. A query string can carry anything a student typed.
+        path: location.pathname,
+        line: extra && extra.line,
+        col: extra && extra.col,
+        // Coarse, non-identifying: enough to tell "only on the lab iPads".
+        ua: navigator.userAgent.slice(0, 200)
+      },
+      occurred_at: Math.floor(Date.now() / 1000)
+    });
+
+    try {
+      // keepalive lets the report survive the page being closed, which is
+      // exactly when the interesting errors happen.
+      fetch(ENDPOINT, {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + KEY },
+        body: body,
+        keepalive: true,
+        mode: 'cors'
+      })['catch'](function () {});
+    } catch (e) {
+      // Nothing to do. A page must not break because it could not report.
+    }
+  }
+
+  window.addEventListener('error', function (e) {
+    // Resource load failures (img/script 404) surface here with no message and
+    // are noise; the service would file them P4 anyway, so save the request.
+    if (!e.message) return;
+    send(e.message, e.error && e.error.stack, { line: e.lineno, col: e.colno });
+  });
+
+  window.addEventListener('unhandledrejection', function (e) {
+    var r = e.reason;
+    send(
+      (r && r.message) || String(r),
+      r && r.stack,
+      {}
+    );
+  });
+
+  // Manual reporting, for a caught error worth knowing about:
+  //   window.lmhsReport(err)
+  window.lmhsReport = function (err, context) {
+    send((err && err.message) || String(err), err && err.stack, context || {});
+  };
+})();
diff --git a/public/units.html b/public/units.html
index d936c61..bf575bb 100644
--- a/public/units.html
+++ b/public/units.html
@@ -6,6 +6,7 @@
 <title>What we cover · Personal Finance · Room 1-240</title>
 <meta name="description" content="The topics covered in the Personal Finance courses in Room 1-240 at LMHS — from the paycheck to credit, debt, investing, taxes, insurance, and the first year on your own.">
 <link rel="stylesheet" href="/styles.css">
+  <script src="/telemetry.js"></script>
 <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' rx='22' fill='%231a7f5a'/%3E%3Ctext x='50' y='72' font-size='62' font-family='Georgia,serif' font-weight='700' text-anchor='middle' fill='%23fff'%3E%24%3C/text%3E%3C/svg%3E">
 <script>
 /* Runs before first paint. app.js is deferred, so leaving this to it meant a
